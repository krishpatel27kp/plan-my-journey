// server/routes/cities.js
// Pillar C — Discovery: City & Activity endpoints
// Phase 0 scaffold — empty router skeleton
//
// Planned endpoints (Phase 1+):
//   GET /api/cities              — list/search cities (public, no auth)
//   GET /api/cities/:cityId/activities — list activities for a city (public, no auth)

const express = require('express');
const router = express.Router();

// TODO: Phase 1 — implement GET /api/cities
// TODO: Phase 1 — implement GET /api/cities/:cityId/activities

module.exports = router;
