# plan-my-journey

A collaborative trip planning web application.

## Getting Started

```bash
cp .env.example .env
# Fill in your local values in .env
npm install
npm run dev
```

## Project Structure

```
client/     — Frontend (Vite + React)
server/     — Backend (Express)
database/   — SQL schema and migrations
```
